- name: Demo on conditions 
  hosts: all
  tasks:
   - man: Execute this task when Distribution is CentOS
     ansible.builtin.debug:
      msg: Welcome to CentOS
     when: ansible_distribution=="CentOS"